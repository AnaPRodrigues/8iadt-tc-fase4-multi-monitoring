.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/

# Saídas de execução — nunca versionadas
output/
*.egg-info/

# Datasets baixados por F0 — mantém só o README (o script vive em backend/scripts/)
data/*
!data/README.md

# Pesos treinados (AD-043) — mantém só o README, peso publicado como GitHub Release
models/*
!models/README.md

# Subconjunto de imagens preparado localmente para subir ao Drive (AD-042)
training/staging/

# Frontend
node_modules/
frontend/dist/
frontend/build/

# Config de ambiente (AD-037) — só .env.example é versionado
.env
.env.local
.env.cloud

# Estado do LocalStack (AD-034)
.localstack/

